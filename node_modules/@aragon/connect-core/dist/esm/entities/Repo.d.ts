import { AragonArtifact, AragonArtifactRole, AragonManifest, Metadata } from '../types';
import Organization from './Organization';
export interface RepoData {
    address: string;
    artifact?: string | null;
    contentUri?: string;
    manifest?: string | null;
    name: string;
    registry?: string;
    registryAddress?: string;
}
export default class Repo {
    #private;
    readonly address: string;
    readonly contentUri?: string;
    readonly name: string;
    readonly registry?: string;
    readonly registryAddress?: string;
    constructor(data: RepoData, metadata: Metadata, organization: Organization);
    static create(data: RepoData, organization: Organization): Promise<Repo>;
    get artifact(): AragonArtifact;
    get manifest(): AragonManifest;
    get roles(): AragonArtifactRole[];
}
//# sourceMappingURL=Repo.d.ts.map