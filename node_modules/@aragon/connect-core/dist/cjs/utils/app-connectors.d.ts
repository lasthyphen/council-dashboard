import { Network } from '@aragon/connect-types';
import { ConnectionContext } from '../types';
import App from '../entities/App';
declare type AppConnectContext = {
    app: App;
    config: object;
    connector: string;
    ipfs: ConnectionContext['ipfs'];
    network: Network;
    orgConnector: ConnectionContext['orgConnector'];
    verbose: boolean;
};
export declare function createAppConnector<ConnectedApp extends object, Config extends object>(callback: (context: {
    config: Config;
} & AppConnectContext) => ConnectedApp | Promise<ConnectedApp>): (app: App | Promise<App>, connector?: string | [string, Config | undefined] | undefined) => Promise<App & ConnectedApp>;
export {};
//# sourceMappingURL=app-connectors.d.ts.map